name: Flutter Android CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-android:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: "temurin"
          java-version: "17"
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.41.2"
          channel: "stable"
      - name: Pub get
        run: flutter pub get
        working-directory: adb_phone_control
      - name: Analyze
        run: flutter analyze
        working-directory: adb_phone_control
      - name: Test
        run: flutter test
        working-directory: adb_phone_control
      - name: Build APK
        run: flutter build apk --release
        working-directory: adb_phone_control
